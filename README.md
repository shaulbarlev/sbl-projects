- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
#- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
S- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
[- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
]- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
q- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
2- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
D- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
x- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
M- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
;- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
+- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
z- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
;- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
+- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
0- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
T- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
;- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
C- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
I- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
L- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
I- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
"- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
C- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
V- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
"- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
x- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
&- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
x- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
T- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
x- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
;- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
T- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
"- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
&- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
"- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
1- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
6- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
9- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
T- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
7- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
0- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
M- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
B- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
'- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
2- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
5- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
M- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
B- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
P- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
A- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
T- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
I- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
S- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
L- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
A- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
N- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
D- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
S- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
F- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
D- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
T- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
P- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
*- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
U- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
R- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
L- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
<- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
>- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
O- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
x- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
P- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
H- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
L- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
E- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
D- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
V- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
+- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
T- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
S- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
D- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
C- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
W- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
B- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
#- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
#- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
A- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
1- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
P- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
<- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
>- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
4- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
2- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
A- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
O- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
R- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
D- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
E- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
R- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
=- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
3- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
q- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
z- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
T- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
T- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
#- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
#- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
L- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
z- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
-- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
(- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
)- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
x- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
z- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
b- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
:- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
'- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
q- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
u- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
v- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
f- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
<- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
>- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
/- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
x- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
.- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
`- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
,- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
p- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
j- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
U- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
R- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
L- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
k- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
y- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
c- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
s- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
h- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
r- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
o- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
w- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
t- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
l- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
n- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
d- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
i- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
m- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
a- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
g- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
e- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
 - **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
|- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.

- **Projects** live in the map. Tapping a tile grows it, where it stands, into a
  page-shaped card with the tile's picture as its header (`src/card.ts`). On a
  wide screen the card fills most of the view and scrolls inside; on a phone it
  is as tall as its page and is read by panning the map, its top in view first.
  Panning away folds it back into the tile. Every project still has its own URL,
  `/<id>/`, which opens its card; `/pikud` is an extra entry for Pikud HaoLED.
